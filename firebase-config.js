// Public Firebase web configuration.
// These values identify the project; they are not secrets. Access control is
// enforced by Firestore security rules (public read, single-user write).
// Find them in Firebase console > Project settings > General > Your apps.
window.FIREBASE_CONFIG = {
    projectId: 'YOUR_FIREBASE_PROJECT_ID',
    apiKey: 'YOUR_FIREBASE_WEB_API_KEY'
};
